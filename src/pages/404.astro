---
/**
 * `/404` — the English 404 page. Astro's special filename: the Cloudflare
 * Pages adapter serves this for any unmatched route. The body lives in
 * `src/components/pages/NotFound.astro`, shared with the localised
 * `src/pages/[lang]/404.astro` routes (DEV-83).
 */

import NotFound from "~/components/pages/NotFound.astro";
---

<NotFound />
