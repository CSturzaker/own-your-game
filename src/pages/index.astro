---
/**
 * `/` — the English home page. The body lives in
 * `src/components/pages/Home.astro`, shared with the localised
 * `src/pages/[lang]/index.astro` routes (DEV-69).
 */

import Home from "~/components/pages/Home.astro";
---

<Home />
