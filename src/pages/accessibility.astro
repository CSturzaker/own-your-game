---
/**
 * `/accessibility` — the English Accessibility Statement. The body lives
 * in `src/components/pages/LegalDocument.astro`, shared with the localised
 * `src/pages/[lang]/accessibility.astro` routes (DEV-82).
 */

import LegalDocument from "~/components/pages/LegalDocument.astro";
---

<LegalDocument slug="accessibility" />
