# Vendored / read-only reference
design/**

# Token mirror — must stay byte-identical to design/handoff/project/hifi-tokens.css.
# Prettier lowercases hex values and reformats whitespace, which would
# break the parity that tests/unit/styles/design-tokens.test.ts enforces.
src/styles/global.css

# Local-only / generated tooling state
.claude/
.vscode/

# Build output
dist/
.astro/

# Dependencies
node_modules/
pnpm-lock.yaml

# Test + coverage artefacts
playwright-report/
test-results/
playwright/.cache/
coverage/

# Lighthouse CI artefacts
.lighthouseci/

# Generated content
content/voices.json

# Hand-edited editorial markdown — translators set their own line
# breaks, and prettier mangles the `::values` list block by
# indenting the closing fence into the list's continuation column.
content/letter/

# Legal prose (DEV-82) — UNICEF-approved / sign-off-pending copy that
# must stay byte-stable; we never reformat the words. Same treatment as
# content/letter/ above.
content/legal/
